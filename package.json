{
  "name": "satisfactory-blueprint",
  "version": "0.0.1",
  "description": "Exchangeable blueprints of Satisfactory factories",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "repository": "https://github.com/bitowl/satisfactory-blueprint",
  "author": "bitowl",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^12.0.4",
    "json-schema-to-typescript": "^6.1.3",
    "tslint": "^5.17.0",
    "typescript": "^3.5.1"
  },
  "scripts": {
    "generateSchema": "json2ts docs/schema-0.0.1.json src/schema.d.ts",
    "build": "tsc",
    "build:watch": "tsc --watch",
    "lint": "tslint --project \"./tsconfig.json\"",
    "prepublish": "yarn build",
    "postversion": "git push && git push --tags"
  },
  "files": [
    "lib/**/*"
  ]
}
